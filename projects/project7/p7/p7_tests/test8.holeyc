int myFunc(int a, int b, int c, int d, int e, int f, int g, int h, int i) {
	return (a + b + c + d + e + f + g + h + i);
}

int toInt(bool b) {
	if(b) { return 1; }
	return 0;
}

int main() {
	bool b;
	b = (myFunc(1,1,1,1,1,1,1,1,1) == 9);
	if(b) {
		TOCONSOLE "true";
	} else {
		TOCONSOLE "false";
	}
	return toInt(b);
}
