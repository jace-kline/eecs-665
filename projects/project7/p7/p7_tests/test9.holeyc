int myFunc(int a, int b, int c, int d, int e, int f) {
	return (a + b + c + d + e + f);
}

int toInt(bool b) {
	if(b) { return 1; }
	return 0;
}

int main() {
	bool b;
	b = (myFunc(1,1,1,1,1,1) == 6);
	if(b) {
		TOCONSOLE "true";
	} else {
		TOCONSOLE "false";
	}
	return toInt(b);
}
